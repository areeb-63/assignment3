name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Flask API image
        uses: docker/build-push-action@v5
        with:
          context: ./app/flask-api
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/flask-api:latest

      - name: Build and push Nginx image
        uses: docker/build-push-action@v5
        with:
          context: ./app/nginx
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/nginx-proxy:latest

      - name: Verify images on DockerHub
        run: |
          echo "Flask API image: ${{ secrets.DOCKERHUB_USERNAME }}/flask-api:latest"
          echo "Nginx image: ${{ secrets.DOCKERHUB_USERNAME }}/nginx-proxy:latest"
          echo "Pipeline completed successfully!"
